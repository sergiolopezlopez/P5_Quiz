<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <table>
        <% for (var i in quizzes){%>
        <% var quiz= quizzes[i];%>
        <li>
            <%=quiz.question%>: <%=quiz.answer%>
        </li>
        <%}%>
    </table>
    <a href="/credits"><button>Página credits</button></a>
        <a href="/quizzes"><button>Página quizzes</button></a>
  </body>
</html>
